<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        block metatags
        link(rel="stylesheet", href="./style.css")
        title Proyecto ToDo
    body
        block content
        script(src="/reload/reload.js") <!-- Permite hacer el live reload con el pluging ack reload -->
        script(src="./script.js") 